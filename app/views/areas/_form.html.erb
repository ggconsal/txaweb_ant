<%= form_for(@area) do |f| %>
  <% if @area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@area.errors.count, "error") %> prohibited this area from being saved:</h2>

      <ul>
      <% @area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :are_titulo %><br>
    <%= f.text_field :are_titulo %>
  </div>
  <div class="field">
    <%= f.label :are_desc %><br>
    <%= f.text_area :are_desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
